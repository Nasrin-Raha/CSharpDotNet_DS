// Anonymous and delegets

// anonymouse are functions without name 
    
    FUNC<int, int> square = delegate(int x){
        return x * x;
    };

Console.WriteLine(square(5)); // Output: 25


// lambda expression

Func<int, int> square = x => x * x;
Console.WriteLine(square(5)); // Output: 25
